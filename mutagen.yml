sync:
    app:
      mode: "two-way-resolved"
      alpha: "app"
      beta: "my-app:/home/vagrant/app"
      ignore:
        vcs: true
        paths:
          - "/node_modules"
          - "/log"
          - "/tmp"